# Production Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:oracle:thin:@laboratorymanagement_high}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:smartstock_db}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:SmartStock.2026!}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

smartstock.jwt.secret=${SMARTSTOCK_JWT_SECRET:smartstock_super_secret_key_2026_jwt_token_must_be_long_enough}
smartstock.jwt.expiration=86400000
